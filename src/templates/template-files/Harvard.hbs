<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/example.css">
    <style>
        {{{css}}}
        body {
            font-family: 'Times New Roman', serif;
        }

        a {
            color: black;
        }

        h1 {
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
        }
        p,li{
            font-size: 11px;
            text-align: left;
        }

        .headerCV{
            margin: 40px 0;
            display: flex;
            flex-direction: column;
        }

        .contact__info {
            font-size: 16px;
        }

        @page{
            size: A4;
            margin: 20mm;
        }


        .subtitle {
            text-align: center;
            font-size: 14px;
            font-weight: bold;
            width: 100%;
            text-transform: uppercase;
        }

        .subtitle.underlined::after {
            height: 1px;
            width: 100%;
            display: block;
            content: "";
            background-color: black;
        }

        .resume {
            line-height: 1.4;
        }

        .subtitleInner h2{
            font-size: 12px;
            font-weight: bold;
        }

        .date {
            font-size: 10px;
            font-weight: lighter;
        }

        .workExperience {
            margin: 8px 0;
        }

        .titleWithDateInfo {
            display: flex;
            justify-content: space-between;
        }

        .titleWithDateInfo .subtitle {
            font-size: 12px;
            text-align: left;
            font-weight: bold;
        }

        .mainElement{
            margin: 15px 0;
        }

        .grid {
            display: grid;
        }

        .grid-cols-4 {
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
        }

        .sectionSpacing {
            margin: 25px 0;
        }
    </style>
</head>

<body>
    <div class="headerCV">
        <h1 class="text-center margin-0 padding-0">{{data.fullname}}</h1>
        <h3 class="text-center margin-0 padding-0 contact__info">{{data.email}} - {{data.phoneNumber}} - {{data.residence}}</h3>
    </div>
    <section class="sectionSpacing">
        <h2 class="subtitle underlined">{{data.t.summary}}</h2>
        <p class="resume">{{data.resume}}</p>
    </section>

    <section class="sectionSpacing">
        <h2 class="subtitle underlined">{{data.t.workExperience}}</h2>
        {{#each data.workExperience}}
            <div class="mainElement">
                <div class="titleWithDateInfo">
                    <div>
                        <h2 class="subtitle margin-0">{{companyName}}</h2>
                        <p class="margin-0">{{occupation}}</p>
                    </div>
                    <div>
                        <h3 class="date">{{rangeDate}}</h3>
                    </div>
                </div>
                <article>
                    {{#if achievements}}
                        <ul>
                            {{#each achievements}}
                            <li>{{this}}</li>
                            {{/each}}
                        </ul>
                    {{/if}}
                </article>
            </div>
        {{/each}}
    </section>
    
    <section class="sectionSpacing">
        <h2 class="subtitle underlined">{{data.t.education}}</h2>
        {{#each data.education}}
        <div class="mainElement">
            <div class="titleWithDateInfo">
                <div>
                    <h2 class="subtitle margin-0 padding-0">{{institutionName}}</h2>
                    <p class="margin-0 padding-0">{{titleName}}</p>
                    <p class="margin-0 padding-0">{{type}}</p>
                </div>
                <p>{{graduationDate}}</p>
            </div>
        </div>
        {{/each}}
    </section>
    
    {{#if data.projects}}
        <section class="sectionSpacing">
            <h2 class="subtitle underlined">{{data.t.projects}}</h2>
            <div class="grid grid-cols-4">
                {{#each data.projects}}
                    <div class="">
                        {{#if link}}
                            <a href={{link}}>{{title}}</a>
                        {{else}}
                            <h2 class="subtitle">{{title}}</h2>
                        {{/if}}
                        <p>{{description}}</p>
                    </div>
                {{/each}}
            </div>
        </section>
    {{/if}}
    {{#if data.languages}}
        <h2 class="subtitle underlined">{{data.t.languages}}</h2>
        <ul>
            {{#each data.languages}}
                <li>
                    {{name}} - {{level}}
                </li>
            {{/each}}
        </ul>
    {{/if}}
</body>

</html>