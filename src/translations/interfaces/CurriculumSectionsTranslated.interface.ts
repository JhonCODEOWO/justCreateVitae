/**
 * Contract that defines how should be the translation object of every language dictionary for `CurriculumSectionDictionary`
 */
export interface CurriculumSectionsTranslated {
  summary: string;
  workExperience: string;
  projects: string;
  skills: string;
  education: string;
  languages: string;
  contact: string;
}
